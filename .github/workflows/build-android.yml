name: Build Android APK

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Fix line endings
        run: |
          sudo apt-get update && sudo apt-get install -y dos2unix
          dos2unix MeshMap/patches/*.patch
          dos2unix MeshMap/android/gradlew

      - name: Install dependencies
        run: |
          cd MeshMap
          npm install

      - name: Build Android Release APK
        run: |
          cd MeshMap/android
          chmod +x ./gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeshMap-Release-APK
          path: MeshMap/android/app/build/outputs/apk/release/app-release.apk
          retention-days: 7
